default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: time_off_development
  host: localhost

test:
  <<: *default
  database: time_off_test
  host: localhost

production:
  url: <%= ENV['DATABASE_URL'] %>
